<h1>Categories</h1>

<table class="table">
  <tr>
    <th>Category</th>
    <th>Delete</th>
    <th>Items</th>
  </tr>
  <% @categories.each do |c| %>
  <tr>
    <td>
      <%= link_to c.title, c, class: "lead-column" %>
    </td>
    <td>
      <%= link_to category_path(c), method: :delete, class: "btn btn-danger" do %>
          Delete
        <% end %>
    </td>
    <td>
      <% c.items.each do |item| %>
        <%= link_to item.title, item %> | 
      <% end %>
    </td>

  </tr>
  <% end %>

</table>

<%= link_to "Create New Category", new_category_path, class: "btn btn-primary btn-large" %>
